# Phase 1 & 2 (data pipeline, ML models, Streamlit dashboard)
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
streamlit>=1.30

# Phase 3 (FastAPI backend serving the web app)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# optional — only needed if you swap forecast_price() for the ARIMA alternative
# statsmodels>=0.14
# prophet>=1.1
